# --- The Core Framework ---
# We use >= to get the latest stable features
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# --- The "Vision" Engine (Docling) ---
# Unpinned version to avoid "not found" errors. 
# Docling will download its own models on first run.
docling>=2.0.0
langchain-docling>=0.2.0

# --- The "Brain" (Gemini Flash) ---
langchain-google-genai>=2.0.0

# --- The Search Engine (Free Local Embeddings) ---
# This allows us to run "all-MiniLM-L6-v2" on your CPU for free.
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# --- The Database (Supabase) ---
supabase>=2.0.0

# --- Utilities ---
python-dotenv
pydantic-settings>=2.0.0